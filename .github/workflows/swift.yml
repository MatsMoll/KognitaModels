name: DevelopmentTestsAction

on:
  pull_request:
    branches: 
      - develop
  push:
    branches: 
      - develop

jobs:
  bionic-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1.2.0
    - name: Run unit tests
      run: swift test --enable-test-discovery
